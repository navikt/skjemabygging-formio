# Agent instructions

## Mock forms

Forms are defined programmatically using TypeScript builders in `mocks/mocks/form-builder/`.
Form fixture `.json` files are deprecated, always use form builders when possible.
