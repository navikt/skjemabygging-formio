# Test case 23

Verifies the combined `bruker` and `avsender` mapping used when a Fyllut submission contains both user information and a sender component.

- `tc23a`: person sender with an identified concerned user
- `tc23b`: organization sender with an unidentified concerned user
