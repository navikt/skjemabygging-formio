import DefaultSelectAnswer from '../../shared/PdfDefaultSelectAnswer';
import { PdfComponentProps } from '../../types';

const PdfCountrySelect = (props: PdfComponentProps) => {
  return DefaultSelectAnswer(props);
};

export default PdfCountrySelect;
