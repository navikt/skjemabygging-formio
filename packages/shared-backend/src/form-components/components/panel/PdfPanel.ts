import DefaultSection from '../../shared/PdfDefaultSection';
import { PdfComponentProps } from '../../types';

const PdfPanel = (props: PdfComponentProps) => {
  return DefaultSection(props);
};

export default PdfPanel;
