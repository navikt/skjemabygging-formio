import DefaultAnswer from '../../shared/PdfDefaultAnswer';
import { PdfComponentProps } from '../../types';

const PdfPassword = (props: PdfComponentProps) => {
  return DefaultAnswer(props);
};

export default PdfPassword;
