/*
import renderPdf from '../../../render/RenderPdf';
import { PdfComponentProps } from '../../../types';

const PdfPanel = ({ component, submissionPath, componentRegistry }: PdfComponentProps) => {
  const { label, components } = component;

  return {
    label: label ?? '',
    verdiliste: components?.map((component) => {
      return renderPdf({ component: component, submissionPath, componentRegistry });
    }),
  };
};

export default PdfPanel;
*/
