import { PdfComponentProps } from '../../../types';
import DefaultAnswer from '../../shared/pdf/DefaultAnswer';

const PdfTextArea = (props: PdfComponentProps) => {
  return DefaultAnswer(props);
};

export default PdfTextArea;
