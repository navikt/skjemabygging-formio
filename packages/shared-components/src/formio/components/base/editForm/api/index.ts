import editFormKey from './editFormKey';
import editFormProperties from './editFormProperties';
import editFormProperty from './editFormProperty';

const editFormApi = {
  key: editFormKey,
  properties: editFormProperties,
  property: editFormProperty,
};

export default editFormApi;
