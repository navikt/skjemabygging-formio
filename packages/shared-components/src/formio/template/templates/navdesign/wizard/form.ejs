<div class="{{ ctx.className }}">
  <section id="maincontent" tabIndex="-1">
    <form aria-labelledby="form-id">
      <div ref="{{ ctx.wizardKey }}" class="mb-14">
        {{ ctx.components }}
      </div>
    </form>
  </section>
</div>
