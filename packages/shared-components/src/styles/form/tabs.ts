const tabs = {
  '& .aksel-tabs__tab.active': {
    // These settings are from Aksel and the rule: aksel-tabs__tab[aria-selected="true"]
    boxShadow: 'inset 0 -3px 0 0 var(--ax-border-accent)',
    color: 'var(--ax-text-neutral)',
  },
};

export default tabs;
