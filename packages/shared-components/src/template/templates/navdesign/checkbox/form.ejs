<div class="navds-checkbox navds-checkbox--medium">
  <{{ctx.input.type}}
  ref="input"
  id="{{ctx.instance.id}}-{{ctx.component.key}}"
  class="navds-checkbox__input checkboks"
  {% for (var attr in ctx.input.attr) { %}
  {{attr}}="{{ctx.input.attr[attr]}}"
  {% } %}
  aria-describedby="{{ ctx.component.description ? `d-${ctx.instance.id}-${ctx.component.key} ` : '' }}e-{{ctx.instance.id}}-{{ctx.component.key}}"
  {% if (ctx.checked) { %}checked=true{% } %}
  />
  {% if (!ctx.self.labelIsHidden()) { %}
  {{ctx.util.getDiffTag(ctx)}}
  <label class="navds-checkbox__label navds-body-short" for="{{ctx.instance.id}}-{{ctx.component.key}}">
    <span class="navds-checkbox__content">
      <span class="navds-body-short">
        {{ctx.t(ctx.input.label)}}
        {% if(ctx.component.validate.required === false) { %}
        ({{ctx.t("valgfritt")}})
        {% } %}
      </span>
    </span>
  </label>
  {% } %}
  </div>
