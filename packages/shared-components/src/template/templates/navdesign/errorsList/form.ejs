<h2 class="navds-error-summary__heading navds-heading navds-heading--small" id="errorListTitle" aria-hidden="true">
  {{ctx.t('error')}}
  {% if (ctx.options.vpat) { %}
  <span ref="errorTooltip" class="{{ctx.iconClass('question-sign')}}" tabindex="0"></span>
  {% } %}
</h2>
<ul aria-labelledby="errorListTitle" autofocus tabindex="-1">
  {% ctx.errors.forEach(function(err, i) { %}
  <li>
    <span
      data-component-key="{{err.keyOrPath}}"
      ref="errorRef"
      class="navds-link"
      tabIndex="0"
      role="link"
    >
      {{err.message}}
    </span>
  </li>
  {% }) %}
</ul>
