<div role="group" class="navds-checkbox-group navds-checkbox-group--medium">
  <div class="label-track-changes">
    <label class="navds-label" id="l-{{ctx.instance.id}}-{{ctx.component.key}}">
      {{ctx.t(ctx.component.label)}}
      {% if(ctx.component.validate.required === false) { %}
        ({{ctx.t("valgfritt")}})
      {% } %}
    </label>
    {{ctx.util.getDiffTag(ctx)}}
  </div>
  {% if (ctx.component.description && ctx.component.descriptionPosition !== "above" ) { %}
    <div class="description">{{ctx.t(ctx.component.description)}}</div>
  {% } %}
  <div class="navds-checkboxes">
  {% ctx.values.forEach(function(item) { %}
    <div class="navds-checkbox navds-checkbox--medium form-check checkbox">
      <input
        ref="input"
        type="checkbox"
        id="{{ctx.id}}{{ctx.row}}-{{item.value}}"
        class="navds-checkbox__input checkboks"
        name="data[{{ctx.key}}][{{item.value}}]"
        {% for (var attr in ctx.input.attr) { %}
        {{attr}}="{{ctx.input.attr[attr]}}"
        {% } %}
        aria-checked="{{ctx.value[item.value] === true}}"
        aria-describedby="{{ctx.key}}-error"
        value="{{item.value}}"
        {% if (ctx.value[item.value] === true) { %}
        checked
        {% } %}
        {% if (item.disabled) { %}
        disabled
        {% } %}
      />
      <label class="navds-checkbox__label navds-body-short" for="{{ctx.id}}{{ctx.row}}-{{item.value}}">{{ctx.t(item.label)}}</label>
    </div>
  {% }) %}
  </div>
</div>
