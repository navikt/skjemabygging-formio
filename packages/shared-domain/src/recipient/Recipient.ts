export interface Recipient {
  recipientId?: string;
  name: string;
  poBoxAddress: string;
  postalCode: string;
  postalName: string;
  createdAt?: string;
  createdBy?: string;
  changedAt?: string;
  changedBy?: string;
}
