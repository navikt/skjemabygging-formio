import { initValues } from './initValues';
import { keys } from './keys';

const externalStorageTexts = {
  initValues,
  keys,
};

export type { Tkey } from './keys';
export default externalStorageTexts;
