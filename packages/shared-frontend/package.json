{
  "name": "@navikt/skjemadigitalisering-shared-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "Frontend code shared between the builder and the form filling app",
  "repository": {
    "type": "git",
    "url": "https://github.com/navikt/skjemabygging-formio.git",
    "directory": "packages/shared-frontend"
  },
  "type": "module",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "license": "SEE LICENSE IN LICENSE.md",
  "scripts": {
    "build": "pnpm exec tsc && pnpm exec vite build",
    "check-types": "pnpm exec tsc",
    "clean": "rimraf dist coverage node_modules",
    "watch": "vite build --watch",
    "test": "vitest --run",
    "test:coverage": "vitest --run --coverage",
    "prepare": "pnpm run build"
  },
  "dependencies": {
    "@navikt/aksel-icons": "^8.2.0",
    "@navikt/ds-react": "^8.2.2",
    "@navikt/skjemadigitalisering-shared-domain": "workspace:*",
    "@navikt/skjemadigitalisering-shared-form": "workspace:*",
    "dompurify": "^3.3.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-jss": "^10.10.0",
    "react-router": "^7.12.0"
  },
  "devDependencies": {
    "@testing-library/react": "^16.3.2",
    "@types/node": "^24.12.0"
  }
}
