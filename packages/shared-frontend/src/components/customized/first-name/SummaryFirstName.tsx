import { DefaultAnswer } from '../../shared/form-summary';
import { FormComponentProps } from '../../types';

const SummaryFirstName = (props: FormComponentProps) => {
  return <DefaultAnswer {...props} />;
};

export default SummaryFirstName;
