import { AutomaticProcessing, DataDisclosure, DataStorage, Optional } from './intro/IntroAccordions';
import { BeAwareOf, GuidePanel, ImportantInformation, SelfDeclaration } from './intro/IntroContent';
import { OutOfScope, Prerequisites, Scope } from './intro/IntroSections';

const Intro = {
  GuidePanel,
  ImportantInformation,
  Scope,
  OutOfScope,
  Prerequisites,
  BeAwareOf,
  DataDisclosure,
  DataStorage,
  AutomaticProcessing,
  Optional,
  SelfDeclaration,
};

export default Intro;
