import React, { useMemo } from "react";

function App({ projectURL, store, pusher }) {
  // const { userData } = useAuth();
  //F const formio = useMemo(() => new Formiojs(projectURL), [projectURL]);
  return <div>KRA KRA</div>;
}

export default App;
