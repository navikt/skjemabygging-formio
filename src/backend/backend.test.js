import {Backend} from "./index.js";

describe('Backend', () => {
  it('checks publishing access', async () => {
  });

  xit('gets list of all published forms in github', async () => {

  });

  xit('gets sha of matching form if any', async () => {

  });

  xit('updates form if matching sha', async () => {

  });

  xit('creates new form if not matching sha', async () => {

  });

  xit('updates form if matching sha', async () => {

  });

  xit('creates new form if not matching sha', async () => {

  });
});
